require 'spec_helper'

describe ActiveMeta do
  #it { should autoload :Attribute }
  #it { should autoload :Concern }
  #it { should autoload :Concerns }
  #it { should autoload :Core }
  #it { should autoload :Recipes }
  #it { should autoload :Rule }
end
